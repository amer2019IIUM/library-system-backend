models:
    User:
        id: increments
        username: string
        email: string
        email_verified_at: string nullable
        password: string
        address: string
        lat: string
        lng: string
        isblocked: boolean nullable
    Book:
        id: increments
        book_name: string
        book_desc: text
        author: string
        image: longText
        is_booked: boolean nullable
        user_id: integer
    Category:
        id: increments
        catergory_name: integer
